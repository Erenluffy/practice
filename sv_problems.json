[
  {
    "id": "sv_constraints_array",
    "title": "Array Constraints",
    "description": "Write SystemVerilog code with constraints: array size < 10 and array sum = 100. Create a class with random array and test it.",
    "difficulty": "medium",
    "category": "SystemVerilog",
    "language": "systemverilog",
    "features": ["constraints", "randomization"],
    "template": "class array_constraints;\n    // TODO: Add rand array and constraints\n    // constraint: size < 10\n    // constraint: sum == 100\n    \n    function void display();\n        // Display array\n    endfunction\nendclass\n\nmodule test;\n    initial begin\n        // Test the constraints\n        $display(\"Testing array constraints...\");\n        \n        // Create instance and randomize\n        // Display results\n        \n        $display(\"Test completed\");\n    end\nendmodule",
    "hint": "Use: rand int array[]; constraint c_size { array.size() < 10; } constraint c_sum { array.sum() == 100; }"
  },
  {
    "id": "sv_assert_simple",
    "title": "Simple Assertions",
    "description": "Write a SystemVerilog assertion to check that data is never X when valid is high.",
    "difficulty": "easy",
    "category": "SystemVerilog",
    "language": "systemverilog",
    "features": ["assertions"],
    "template": "module data_checker(\n    input logic clk,\n    input logic rst_n,\n    input logic [7:0] data,\n    input logic valid\n);\n    // TODO: Add assertion\n    // Should check: valid -> !$isunknown(data)\n    \n    // Also add manual check for debugging\n    always @(posedge clk) begin\n        if (valid && $isunknown(data)) begin\n            $display(\"ERROR: Unknown data when valid!\");\n        end\n    end\nendmodule\n\nmodule test;\n    logic clk = 0;\n    logic rst_n = 1;\n    logic [7:0] data;\n    logic valid;\n    \n    data_checker dut (.*);\n    \n    always #5 clk = ~clk;\n    \n    initial begin\n        $display(\"Testing assertions...\");\n        \n        // Test cases\n        valid = 1; data = 8'hFF; #10;\n        valid = 1; data = 8'hXX; #10;\n        valid = 0; data = 8'hZZ; #10;\n        \n        $display(\"Test completed\");\n        $finish;\n    end\nendmodule",
    "hint": "Use: assert property (@(posedge clk) disable iff (!rst_n) valid |-> !$isunknown(data));"
  }
]
