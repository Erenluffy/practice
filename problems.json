[
  {
    "id": "and_gate",
    "title": "AND Gate",
    "description": "Implement a 2-input AND gate. Output is high only when both inputs are high.",
    "difficulty": "easy",
    "category": "basic_gates",
    "template": "module top_module(input a, b, output out);\n  // Your code here\n  // assign out = a & b;\nendmodule",
    "testbench": "`timescale 1ns/1ps\nmodule tb();\n    reg a, b;\n    wire out;\n    integer passed = 0;\n    top_module dut (.a(a), .b(b), .out(out));\n    initial begin\n        $display(\"Testing AND Gate...\");\n        a=0; b=0; #10; if(out !== 0) $display(\"FAIL 00\"); else passed++;\n        a=0; b=1; #10; if(out !== 0) $display(\"FAIL 01\"); else passed++;\n        a=1; b=0; #10; if(out !== 0) $display(\"FAIL 10\"); else passed++;\n        a=1; b=1; #10; if(out !== 1) $display(\"FAIL 11\"); else passed++;\n        if(passed==4) $display(\"PASS\"); else $display(\"FAIL\");\n        $finish;\n    end\nendmodule",
    "hint": "Use bitwise AND operator &",
    "examples": [
      {
        "input": "a=0,b=0",
        "output": "out=0",
        "explanation": "Both low, output low"
      },
      {
        "input": "a=1,b=1",
        "output": "out=1",
        "explanation": "Both high, output high"
      }
    ],
    "test_cases": [
      {
        "input": {
          "a": 0,
          "b": 0
        },
        "expected": {
          "out": 0
        },
        "description": "Both inputs low"
      },
      {
        "input": {
          "a": 1,
          "b": 1
        },
        "expected": {
          "out": 1
        },
        "description": "Both inputs high"
      }
    ],
    "constraints": [
      "Combinational logic only",
      "Use continuous assignment"
    ]
  },
  {
    "id": "or_gate",
    "title": "OR Gate",
    "description": "Implement a 2-input OR gate. Output is high when at least one input is high.",
    "difficulty": "easy",
    "category": "basic_gates",
    "template": "module top_module(input a, b, output out);\n  // assign out = a | b;\nendmodule",
    "testbench": "`timescale 1ns/1ps\nmodule tb();\n    reg a, b;\n    wire out;\n    integer passed = 0;\n    top_module dut (.a(a), .b(b), .out(out));\n    initial begin\n        $display(\"Testing OR Gate...\");\n        a=0; b=0; #10; if(out !== 0) $display(\"FAIL 00\"); else passed++;\n        a=0; b=1; #10; if(out !== 1) $display(\"FAIL 01\"); else passed++;\n        a=1; b=0; #10; if(out !== 1) $display(\"FAIL 10\"); else passed++;\n        a=1; b=1; #10; if(out !== 1) $display(\"FAIL 11\"); else passed++;\n        if(passed==4) $display(\"PASS\"); else $display(\"FAIL\");\n        $finish;\n    end\nendmodule",
    "hint": "Use bitwise OR operator |",
    "examples": [
      {
        "input": "a=0,b=1",
        "output": "out=1",
        "explanation": "One input high, output high"
      }
    ],
    "test_cases": [
      {
        "input": {
          "a": 0,
          "b": 0
        },
        "expected": {
          "out": 0
        },
        "description": "Both inputs low"
      },
      {
        "input": {
          "a": 0,
          "b": 1
        },
        "expected": {
          "out": 1
        },
        "description": "At least one input high"
      }
    ],
    "constraints": [
      "Combinational logic only"
    ]
  },
  {
    "id": "nand_gate",
    "title": "NAND Gate",
    "description": "Implement a 2-input NAND gate. Output is low only when both inputs are high.",
    "difficulty": "easy",
    "category": "basic_gates",
    "template": "module top_module(input a, b, output out);\n  // assign out = ~(a & b);\nendmodule",
    "testbench": "`timescale 1ns/1ps\nmodule tb();\n    reg a, b;\n    wire out;\n    integer passed = 0;\n    top_module dut (.a(a), .b(b), .out(out));\n    initial begin\n        $display(\"Testing NAND Gate...\");\n        a=0; b=0; #10; if(out !== 1) $display(\"FAIL 00\"); else passed++;\n        a=0; b=1; #10; if(out !== 1) $display(\"FAIL 01\"); else passed++;\n        a=1; b=0; #10; if(out !== 1) $display(\"FAIL 10\"); else passed++;\n        a=1; b=1; #10; if(out !== 0) $display(\"FAIL 11\"); else passed++;\n        if(passed==4) $display(\"PASS\"); else $display(\"FAIL\");\n        $finish;\n    end\nendmodule",
    "hint": "NAND = NOT AND",
    "examples": [
      {
        "input": "a=1,b=1",
        "output": "out=0",
        "explanation": "NAND of ones is zero"
      }
    ],
    "test_cases": [
      {
        "input": {
          "a": 1,
          "b": 1
        },
        "expected": {
          "out": 0
        },
        "description": "NAND of ones is zero"
      },
      {
        "input": {
          "a": 0,
          "b": 0
        },
        "expected": {
          "out": 1
        },
        "description": "NAND of zeros is one"
      }
    ],
    "constraints": [
      "Combinational logic only"
    ]
  },
  {
    "id": "nor_gate",
    "title": "NOR Gate",
    "description": "Implement a 2-input NOR gate. Output is high only when both inputs are low.",
    "difficulty": "easy",
    "category": "basic_gates",
    "template": "module top_module(input a, b, output out);\n  // assign out = ~(a | b);\nendmodule",
    "testbench": "`timescale 1ns/1ps\nmodule tb();\n    reg a, b;\n    wire out;\n    integer passed = 0;\n    top_module dut (.a(a), .b(b), .out(out));\n    initial begin\n        $display(\"Testing NOR Gate...\");\n        a=0; b=0; #10; if(out !== 1) $display(\"FAIL 00\"); else passed++;\n        a=0; b=1; #10; if(out !== 0) $display(\"FAIL 01\"); else passed++;\n        a=1; b=0; #10; if(out !== 0) $display(\"FAIL 10\"); else passed++;\n        a=1; b=1; #10; if(out !== 0) $display(\"FAIL 11\"); else passed++;\n        if(passed==4) $display(\"PASS\"); else $display(\"FAIL\");\n        $finish;\n    end\nendmodule",
    "hint": "NOR = NOT OR",
    "examples": [
      {
        "input": "a=0,b=0",
        "output": "out=1",
        "explanation": "Both low, output high"
      }
    ],
    "test_cases": [
      {
        "input": {
          "a": 0,
          "b": 0
        },
        "expected": {
          "out": 1
        },
        "description": "Both low, output high"
      },
      {
        "input": {
          "a": 1,
          "b": 0
        },
        "expected": {
          "out": 0
        },
        "description": "One high, output low"
      }
    ],
    "constraints": [
      "Combinational logic only"
    ]
  },
  {
    "id": "xor_gate",
    "title": "XOR Gate",
    "description": "Implement a 2-input XOR gate. Output is high when inputs are different.",
    "difficulty": "easy",
    "category": "basic_gates",
    "template": "module top_module(input a, b, output out);\n  // assign out = a ^ b;\nendmodule",
    "testbench": "`timescale 1ns/1ps\nmodule tb();\n    reg a, b;\n    wire out;\n    integer passed = 0;\n    top_module dut (.a(a), .b(b), .out(out));\n    initial begin\n        $display(\"Testing XOR Gate...\");\n        a=0; b=0; #10; if(out !== 0) $display(\"FAIL 00\"); else passed++;\n        a=0; b=1; #10; if(out !== 1) $display(\"FAIL 01\"); else passed++;\n        a=1; b=0; #10; if(out !== 1) $display(\"FAIL 10\"); else passed++;\n        a=1; b=1; #10; if(out !== 0) $display(\"FAIL 11\"); else passed++;\n        if(passed==4) $display(\"PASS\"); else $display(\"FAIL\");\n        $finish;\n    end\nendmodule",
    "hint": "Use ^ operator",
    "examples": [
      {
        "input": "a=1,b=0",
        "output": "out=1",
        "explanation": "Different inputs, output high"
      }
    ],
    "test_cases": [
      {
        "input": {
          "a": 1,
          "b": 0
        },
        "expected": {
          "out": 1
        },
        "description": "Different inputs"
      },
      {
        "input": {
          "a": 1,
          "b": 1
        },
        "expected": {
          "out": 0
        },
        "description": "Same inputs"
      }
    ],
    "constraints": [
      "Combinational logic only"
    ]
  },
  {
    "id": "xnor_gate",
    "title": "XNOR Gate",
    "description": "Implement a 2-input XNOR gate. Output is high when inputs are same.",
    "difficulty": "easy",
    "category": "basic_gates",
    "template": "module top_module(input a, b, output out);\n  // assign out = ~(a ^ b);\nendmodule",
    "testbench": "`timescale 1ns/1ps\nmodule tb();\n    reg a, b;\n    wire out;\n    integer passed = 0;\n    top_module dut (.a(a), .b(b), .out(out));\n    initial begin\n        $display(\"Testing XNOR Gate...\");\n        a=0; b=0; #10; if(out !== 1) $display(\"FAIL 00\"); else passed++;\n        a=0; b=1; #10; if(out !== 0) $display(\"FAIL 01\"); else passed++;\n        a=1; b=0; #10; if(out !== 0) $display(\"FAIL 10\"); else passed++;\n        a=1; b=1; #10; if(out !== 1) $display(\"FAIL 11\"); else passed++;\n        if(passed==4) $display(\"PASS\"); else $display(\"FAIL\");\n        $finish;\n    end\nendmodule",
    "hint": "XNOR = NOT XOR",
    "examples": [
      {
        "input": "a=1,b=1",
        "output": "out=1",
        "explanation": "Same inputs, output high"
      }
    ],
    "test_cases": [
      {
        "input": {
          "a": 1,
          "b": 1
        },
        "expected": {
          "out": 1
        },
        "description": "Same inputs"
      },
      {
        "input": {
          "a": 1,
          "b": 0
        },
        "expected": {
          "out": 0
        },
        "description": "Different inputs"
      }
    ],
    "constraints": [
      "Combinational logic only"
    ]
  },
  {
    "id": "not_gate",
    "title": "NOT Gate (Inverter)",
    "description": "Implement a single input NOT gate. Output is inverted input.",
    "difficulty": "easy",
    "category": "basic_gates",
    "template": "module top_module(input in, output out);\n  // assign out = ~in;\nendmodule",
    "testbench": "`timescale 1ns/1ps\nmodule tb();\n    reg in;\n    wire out;\n    integer passed = 0;\n    top_module dut (.in(in), .out(out));\n    initial begin\n        $display(\"Testing NOT Gate...\");\n        in=0; #10; if(out !== 1) $display(\"FAIL 0\"); else passed++;\n        in=1; #10; if(out !== 0) $display(\"FAIL 1\"); else passed++;\n        if(passed==2) $display(\"PASS\"); else $display(\"FAIL\");\n        $finish;\n    end\nendmodule",
    "hint": "Use ~ operator",
    "examples": [
      {
        "input": "in=1",
        "output": "out=0",
        "explanation": "Invert input"
      }
    ],
    "test_cases": [
      {
        "input": {
          "in": 1
        },
        "expected": {
          "out": 0
        },
        "description": "Invert high to low"
      },
      {
        "input": {
          "in": 0
        },
        "expected": {
          "out": 1
        },
        "description": "Invert low to high"
      }
    ],
    "constraints": [
      "Single input only"
    ]
  },
  {
    "id": "vector_not4",
    "title": "4-bit Vector NOT",
    "description": "Implement a 4-bit vector NOT gate. Invert all bits of input vector.",
    "difficulty": "easy",
    "category": "basic_gates",
    "template": "module vector_not4(input [3:0] in, output [3:0] out);\n  // assign out = ~in;\nendmodule",
    "testbench": "`timescale 1ns/1ps\nmodule tb();\n    reg [3:0] in;\n    wire [3:0] out;\n    integer passed = 0;\n    vector_not4 dut (.in(in), .out(out));\n    initial begin\n        in = 4'b0000; #10; if(out !== 4'b1111) $display(\"FAIL 1\"); else passed++;\n        in = 4'b1010; #10; if(out !== 4'b0101) $display(\"FAIL 2\"); else passed++;\n        in = 4'b1111; #10; if(out !== 4'b0000) $display(\"FAIL 3\"); else passed++;\n        if(passed==3) $display(\"PASS\"); else $display(\"FAIL\");\n        $finish;\n    end\nendmodule",
    "hint": "Use bitwise NOT on vector",
    "examples": [
      {
        "input": "in=4'b1010",
        "output": "out=4'b0101",
        "explanation": "Bitwise NOT operation on vector"
      }
    ],
    "test_cases": [
      {
        "input": {
          "in": "4'b0000"
        },
        "expected": {
          "out": "4'b1111"
        },
        "description": "Invert all zeros to all ones"
      },
      {
        "input": {
          "in": "4'b1010"
        },
        "expected": {
          "out": "4'b0101"
        },
        "description": "Invert alternating pattern"
      }
    ],
    "constraints": [
      "4-bit input/output",
      "Bitwise operation"
    ]
  }
]
